# 🚀 توثيق واجهة برمجة التطبيقات (Sheetly API)

أهلاً بك في التوثيق الرسمي لـ **شيتلي**، المنصة المتخصصة لإدارة ومشاركة الموارد الدراسية لطلاب جامعة بنغازي.

---

## 🔐 الأمن والتوثيق

تستخدم الواجهة نظام **Laravel Sanctum** لتأمين الوصول.

| الميزة | التفاصيل |
| :--- | :--- |
| **نوع التوثيق** | Bearer Token (Sanctum) |
| **الهيدر (Header)** | `Authorization: Bearer {your_token}` |
| **نطاق البريد** | مقتصر على البريد الجامعي `@uob.edu.ly` |
| **الرابط الأساسي** | `https://your-domain.com/api` |

---

## 📌 المسارات العامة (Public)

### 🔑 دورة التحقق والدخول
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `POST` | `/register` | إنشاء حساب (الاسم، البريد الجامعي، كلمة المرور). |
| `POST` | `/register/verify` | تأكيد الحساب باستخدام رمز (OTP) المكون من 4 أرقام. |
| `POST` | `/resend-otp` | إعادة إرسال رمز التحقق للبريد الجامعي. |
| `POST` | `/login` | تسجيل الدخول والحصول على توكن الوصول. |
| `POST` | `/forgot-password` | طلب رابط إعادة تعيين كلمة المرور. |
| `POST` | `/reset-password` | تعيين كلمة مرور جديدة باستخدام الرمز المستلم. |

### 🏛 دليل المواد الأكاديمية
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `GET` | `/subjects` | عرض قائمة المواد (يدعم البحث عبر پارامتر `search`). |
| `GET` | `/subjects/{code}` | عرض ملف المادة (منظم حسب الشباتر، الجزئي، والنهائي). |
| `GET` | `/subjects/{code}/chapters/{num}` | عرض كافة الشيتات المعتمدة لشابتر محدد. |
| `GET` | `/sheets/{id}` | عرض البيانات الوصفية لشيت محدد. |
| `GET` | `/sheets/{id}/download` | زيادة عداد التحميل والحصول على رابط الملف الآمن. |

---

## 👤 عمليات المستخدم (تتطلب تسجيل دخول)

يجب تضمين الهيدر `Authorization` في جميع الطلبات التالية.

| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `POST` | `/logout` | إلغاء توكن الوصول الحالي. |
| `GET` | `/my-sheets` | عرض كافة الموارد التي رفعها المستخدم (بما فيها المعلقة). |
| `POST` | `/sheets/upload` | رفع مورد دراسي جديد (PDF). |
| `DELETE` | `/sheets/{id}` | حذف مورد دراسي (لصاحب الملف أو المشرف فقط). |

---

## 🛠 لوحة الإدارة (للمشرفين فقط)

هذه المسارات محمية بصلاحيات المشرف (Admin).

### 📄 المراجعة والتدقيق
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `GET` | `/admin/sheets/pending` | عرض الموارد التي تنتظر الموافقة. |
| `PATCH` | `/admin/sheets/{id}/approve` | الموافقة على الشيت ونشره للعامة. |
| `PATCH` | `/admin/sheets/{id}/reject` | رفض الشيت وإخفاؤه. |

### 🏛 إدارة المواد
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `POST` | `/admin/subjects` | إضافة مادة أكاديمية جديدة. |
| `PATCH` | `/admin/subjects/{id}` | تحديث بيانات المادة (الاسم/الرمز). |
| `DELETE` | `/admin/subjects/{id}` | حذف مادة من النظام. |

### 👥 إدارة المستخدمين
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `GET` | `/admin/users` | عرض قائمة المستخدمين (مع دعم الترقيم). |
| `POST` | `/admin/users` | إنشاء مستخدم جديد (مفعل تلقائياً). |
| `PATCH` | `/admin/users/{id}` | تحديث بيانات المستخدم أو تغيير الصلاحيات. |
| `DELETE` | `/admin/users/{id}` | حذف حساب مستخدم. |

---

## 📤 مواصفات الطلبات (Requests)

### رفع مورد دراسي (`POST /sheets/upload`)
**نوع المحتوى:** `multipart/form-data`

| الحقل | النوع | مطلوب | الوصف |
| :--- | :--- | :--- | :--- |
| `title` | `string` | نعم | عنوان الملف (حد أقصى 255 حرف). |
| `subject_id` | `integer` | نعم | معرف المادة الصحيح. |
| `type` | `enum` | نعم | أحد القيم: `chapter`, `midterm`, `final`. |
| `chapter_number` | `integer` | لا | يُحسب تلقائياً إذا كان النوع `chapter` ولم يتم إرساله. |
| `file` | `file` | نعم | ملف PDF فقط، الحجم الأقصى 10 ميجا. |

---

## 🌐 أكواد الاستجابة (HTTP Status)

| الكود | المعنى |
| :--- | :--- |
| `200 OK` | تمت العملية بنجاح. |
| `201 Created` | تم إنشاء المورد بنجاح. |
| `401 Unauthorized` | التوكن مفقود أو غير صحيح. |
| `403 Forbidden` | صلاحيات غير كافية أو البريد غير مفعل. |
| `422 Unprocessable` | خطأ في البيانات المرسلة (تحقق من رسائل الخطأ). |
| `429 Too Many Requests` | تجاوز حد المحاولات المسموح به (OTP/دخول). |

---
<div align="center">
  بُني باستخدام Laravel 12 & ❤️ لطلاب جامعة بنغازي
</div>
