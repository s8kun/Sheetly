<div align="center">
  <h1>🚀 مواصفات API نظام شيتلي</h1>
  <p>التوثيق الرسمي لمسارات نظام شيتلي التعليمي</p>
  
  [![Version](https://img.shields.io/badge/API-v1.0-blue?style=flat-square)](#)
  [![Auth](https://img.shields.io/badge/Auth-Sanctum-orange?style=flat-square)](#)
  <br>
  <a href="api.md"><strong>English Version (English)</strong></a>
</div>

---

## 🔐 التوثيق والحماية
يعتمد الـ API على **Laravel Sanctum** لتأمين الوصول.
- **الهيدر (Header):** `Authorization: Bearer {token}`
- **قيود التسجيل:** مسموح فقط للنطاقات التابعة لـ `@uob.edu.ly`.

---

## 📌 ملخص المسارات

### 🔑 الوصول والهوية
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `POST` | `/api/register` | إنشاء حساب طالب جديد |
| `POST` | `/api/login` | تسجيل الدخول والحصول على التوكن |
| `POST` | `/api/logout` | إلغاء الجلسة الحالية |

### 🏛 دليل المواد
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `GET` | `/api/subjects` | عرض كافة المواد (يدعم البحث) |
| `GET` | `/api/subjects/{code}` | عرض ملف المادة (الشباتر/الامتحانات) |
| `GET` | `/api/subjects/{code}/chapters/{num}` | عرض شيتات شابتر محدد |

### 📄 إدارة الموارد
| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `GET` | `/api/sheets/{id}` | عرض تفاصيل المورد الدراسي |
| `GET` | `/api/sheets/{id}/download` | توليد رابط تحميل آمن |
| `POST` | `/api/sheets/upload` | رفع مورد دراسي جديد (PDF) |
| `GET` | `/api/my-sheets` | سجل المرفوعات للمستخدم الحالي |
| `DELETE` | `/api/sheets/{id}` | حذف مورد دراسي |

---

## 🛠 الإدارة (للمشرفين فقط)
هذه المسارات تتطلب صلاحيات **Admin**.

| الطريقة | المسار | الوصف |
| :--- | :--- | :--- |
| `POST` | `/api/admin/subjects` | تسجيل مادة أكاديمية جديدة |
| `PATCH` | `/api/admin/subjects/{code}` | تعديل بيانات مادة |
| `GET` | `/api/admin/sheets/pending` | عرض طابور مراجعة الملفات |
| `PATCH` | `/api/admin/sheets/{id}/approve` | الموافقة على نشر مورد للعامة |
| `PATCH` | `/api/admin/sheets/{id}/reject` | رفض وإخفاء مورد دراسي |

---

## 📤 مواصفات الرفع
لرفع مورد دراسي، أرسل طلب `multipart/form-data` إلى `/api/sheets/upload`:

| الحقل | النوع | مطلوب | ملاحظات |
| :--- | :--- | :--- | :--- |
| `title` | `string` | نعم | بحد أقصى 255 حرف |
| `subject_id` | `int` | نعم | معرف مادة صحيح |
| `type` | `enum` | نعم | `chapter`, `midterm`, `final` |
| `chapter_number` | `int` | مشروط | مطلوب إذا كان النوع `chapter` |
| `file` | `file` | نعم | ملف PDF فقط، حجم أقصى 20 ميجا |

---

## 🌐 أكواد الاستجابة
| الكود | المعنى |
| :--- | :--- |
| `200/201` | نجاح / تم الإنشاء |
| `401` | غير موثق (التوكن مفقود أو غير صحيح) |
| `403` | وصول مرفوض (صلاحيات غير كافية) |
| `422` | خطأ في البيانات (تحقق من البارامترات) |

---
<div align="center">
  بُني باستخدام Laravel 12 & ❤️
</div>
